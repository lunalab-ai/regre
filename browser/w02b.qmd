---
title: 2주차 2차시 · R 문법 복습 ②
resources: &id001
- data/w02b-observations.csv
- data/r-syntax-tools.R
webr:
  resources: *id001
  packages: []
---

{{< include _extensions/r-wasm/live/_knitr.qmd >}}

R 설치나 로그인이 필요 없습니다. 로딩이 끝나면 **Run Code**를 눌러 위에서부터 실행하세요. 앞 셀의 값을 바꾼 뒤에는 뒤의 계산도 다시 실행해야 합니다. 빈칸·디버깅 문제는 직접 고쳐 실행합니다.

<button type="button" id="download-code" class="btn btn-primary">내 R 코드 다운로드</button> <button type="button" onclick="location.reload()" class="btn btn-outline-secondary">R 세션 다시 시작</button>

실행한 코드는 이 브라우저에 보관됩니다. R 객체는 새로고침 후 다시 만들어야 합니다. 다른 컴퓨터로 옮기거나 제출할 때는 코드를 다운로드하세요.

[기존 R·QMD 다운로드](https://github.com/lunalab-ai/regre/tree/main/labs/student/w02b)


# 시작하기

이 실습은 W02A의 벡터와 데이터프레임에서 출발해 행렬·리스트·요인, 파일 입출력,
조건문·반복문·함수로 이어집니다. 자료는 수업용 합성 자료이며 실제 업무 기록이 아닙니다.
각 코드의 결과를 먼저 예상하고 **Run** 하세요.

## 공통 R 파일을 읽는 순서

`source(tools_path)`는 해당 R 파일의 함수 정의를 현재 실행 환경에 읽어 들이며, 함수 실행이나 학습을 대신하지 않는다. tools_path는 파일 경로 문자열이다. `classify_minutes(minutes, threshold=45)`를 그 다음 호출하면 숫자 벡터와 기준값으로 ordered factor를 반환한다. 결측은 실제 NA가 아닌 missing 범주로 표시한다. `summarize_by_shift(data)`는 shift/minutes 열의 표를 받아 shift/n_observed/mean_minutes 표를 새로 반환한다. minutes는 숫자이며 전부 결측인 집단 평균은 NaN이다. 두 함수는 원본 입력을 변경하지 않는다.

- [classify_minutes 정의·인자·출력](https://github.com/lunalab-ai/regre/blob/main/src/r-syntax-tools.R#L15)
- [summarize_by_shift 정의·인자·출력](https://github.com/lunalab-ai/regre/blob/main/src/r-syntax-tools.R#L36)

::: {#cell-w02b-1 .browser-cell}

```{webr}
#| label: cell-w02b-1
#| autorun: false
#| persist: true
data_path <- file.path("data", "w02b-observations.csv")
tools_path <- file.path("..", "..", "..", "src", "r-syntax-tools.R")
if (!file.exists(tools_path)) tools_path <- file.path("data", "r-syntax-tools.R")
observations <- read.csv(data_path, na.strings = "", stringsAsFactors = FALSE)
source(tools_path)
stopifnot(nrow(observations) == 8)
```

:::

## 1. W02A 회상: 열과 행 선택

다음 두 결과의 자료형과 행 수를 먼저 예상합니다.

::: {#cell-w02b-2 .browser-cell}

```{webr}
#| label: cell-w02b-2
#| autorun: false
#| persist: true
observations$minutes
observations[observations$units >= 5, c("id", "units", "minutes")]
```

:::

`$minutes`는 한 열을 벡터로 꺼냅니다. `[행, 열]`은 조건을 만족하는 행과 필요한 열을
함께 선택합니다. `NA`는 0이 아니라 관측되지 않은 값입니다.

## 2. 같은 값을 담는 서로 다른 구조

### 행렬

행렬은 모든 칸이 같은 자료형인 2차원 구조입니다. 첫 열의 1은 앞으로 회귀모형의
절편과 연결될 수 있지만, 오늘은 구조만 확인합니다.

::: {#cell-w02b-3 .browser-cell}

```{webr}
#| label: cell-w02b-3
#| autorun: false
#| persist: true
design <- cbind(intercept = 1, units = observations$units)
design[1:3, ]
dim(design)
storage.mode(design)
```

:::

### 리스트

리스트는 데이터프레임, 행렬, 문자를 한 객체에 함께 담을 수 있습니다. 이후 `lm()`의
결과처럼 여러 종류의 결과를 묶는 객체를 이해할 때 다시 만납니다.

::: {#cell-w02b-4 .browser-cell}

```{webr}
#| label: cell-w02b-4
#| autorun: false
#| persist: true
lesson_object <- list(
  data = observations,
  design = design,
  note = "synthetic practice data"
)
names(lesson_object)
lesson_object$data$minutes
```

:::

### 요인

요인은 가능한 범주인 `levels`를 가진 범주형 변수입니다. 문자 모양이 같아도 단순
문자 벡터와 역할이 다릅니다.

::: {#cell-w02b-5 .browser-cell}

```{webr}
#| label: cell-w02b-5
#| autorun: false
#| persist: true
observations$shift <- factor(
  observations$shift,
  levels = c("morning", "afternoon", "evening")
)
levels(observations$shift)
table(observations$shift)
```

:::

::: {.callout-note}
행렬은 같은 형식의 직사각형 계산, 리스트는 서로 다른 객체의 묶음, 요인은 범주와
수준의 관리에 적합합니다. 데이터프레임은 열마다 자료형이 달라도 됩니다.
:::

## 3. 파일을 읽고, 점검하고, 다시 저장하기

브라우저에서는 읽기와 구조 점검을 수행합니다. 실제 CSV 쓰기·재읽기는 다운로드한 RStudio 프로젝트에서 실행하세요.

## 4. 비교·논리 연산자와 조건문

`&`와 `|`는 벡터의 각 위치를 비교합니다. `&&`와 `||`는 주로 `if`에서 하나의 판단을
만들 때 사용합니다. 결측은 `== NA`가 아니라 `is.na()`로 찾습니다.

::: {#cell-w02b-6 .browser-cell}

```{webr}
#| label: cell-w02b-6
#| autorun: false
#| persist: true
observed <- !is.na(observations$minutes)
needs_review <- is.na(observations$minutes) |
  observations$minutes < 0 |
  observations$minutes > 120
sum(observed)
which(needs_review)
```

:::

한 값에 대한 분기 흐름입니다.

::: {#cell-w02b-7 .browser-cell}

```{webr}
#| label: cell-w02b-7
#| autorun: false
#| persist: true
threshold <- 45
one_value <- 52
if (is.na(one_value)) {
  "missing"
} else if (one_value <= threshold) {
  "fast"
} else {
  "slow"
}
```

:::

### 오류 해결

목표: 결측을 제외하고 40분 이상 70분 이하인 모든 행을 찾습니다. 힌트: 벡터의 각
위치를 비교하는 연산자는 `&`이고, 결측 제외에는 `!is.na()`가 필요합니다. 수정 뒤
결과가 J04, J05, J06인지 확인하세요.

::: {#cell-w02b-8 .browser-cell}

```{webr}
#| label: cell-w02b-8
#| autorun: false
#| persist: true
#| exercise: ex-w02b-8
observations[
  observations$minutes >= 40 && observations$minutes <= 70,
  c("id", "minutes")
]
```

:::

## 5. 반복문에서 벡터화로

반복문은 계산 순서를 눈에 보이게 합니다. 같은 계산이 각 원소에 독립적으로 적용되면
벡터 연산이 더 간결할 수 있습니다.

::: {#cell-w02b-9 .browser-cell}

```{webr}
#| label: cell-w02b-9
#| autorun: false
#| persist: true
loop_result <- numeric(nrow(observations))
for (i in seq_len(nrow(observations))) {
  loop_result[i] <- observations$units[i] * 10
}
vector_result <- observations$units * 10
stopifnot(identical(loop_result, vector_result))
loop_result
```

:::

## 6. 함수: 입력–처리–반환

공통 파일의 `classify_minutes()`는 숫자 벡터와 기준값을 입력받아 요인을 반환합니다.
결측을 먼저 처리하지 않으면 `if (NA)` 문제가 생길 수 있습니다.

::: {#cell-w02b-10 .browser-cell}

```{webr}
#| label: cell-w02b-10
#| autorun: false
#| persist: true
observations$status <- classify_minutes(observations$minutes, threshold)
table(observations$status)
```

:::

### 빈칸 채우기

목표: 기준을 55분으로 바꾼 분류를 만듭니다. 힌트: 함수의 두 번째 인자는
`threshold`입니다. `table()` 결과가 fast 5, slow 2, missing 1인지 확인하세요.

::: {#cell-w02b-11 .browser-cell}

```{webr}
#| label: cell-w02b-11
#| autorun: false
#| persist: true
#| exercise: ex-w02b-11
status_55 <- classify_minutes(observations$minutes, threshold = ___)
table(status_55)
```

:::

## 7. apply 계열: 입력과 반환 모양 확인

::: {#cell-w02b-12 .browser-cell}

```{webr}
#| label: cell-w02b-12
#| autorun: false
#| persist: true
column_means <- sapply(
  observations[c("units", "minutes")],
  mean,
  na.rm = TRUE
)
row_means <- apply(design, 1, mean)
shift_summary <- summarize_by_shift(observations)
column_means
head(row_means)
shift_summary
```

:::

`apply(design, 1, mean)`의 1은 행 방향, 2는 열 방향입니다. `lapply()`는 리스트를
반환하고 `sapply()`는 가능하면 벡터나 행렬로 단순화합니다. 결과 구조가 중요할 때는
`str()`로 확인합니다.

# 8. R 문법 실험실 앱

[누적 앱 코드 편집기와 실행 화면 열기](apps/w02b/edit/index.html)\n\n첫 회귀 예측 기능을 확인한 뒤 R 문법 실험실 탭에서 임계값과 근무조를 바꾸세요. 기본 임계값 한 줄을 수정하고 Re-run app으로 다시 실행합니다.

# 9. 종료 체크

내 R 코드 다운로드 버튼으로 작업을 저장하세요. R 세션 다시 시작 후 위에서부터 재실행하여 숨은 객체 의존성이 없는지 확인합니다.


```{ojs}
//| echo: false
//| output: false
courseRuntime = { window.courseR = await webROjs.webRPromise; return true; }
```
